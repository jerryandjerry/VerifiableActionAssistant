# Contributing

1. Create a focused branch.
2. Add or update tests for every behavior change.
3. Run `make lint`, `make test`, and `make eval`.
4. Keep write-capable tools behind the policy gateway.
5. Add a regression case when fixing an agent failure.
